version: "3"

services:
  mkdocs:
    build: .
    environment:
      #Application
      - HOST=${HOST:-0.0.0.0}
      - PORT=${PORT:-8000}
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - ./docs:/app/docs
      - ./mkdocs.yml:/app/mkdocs.yml
